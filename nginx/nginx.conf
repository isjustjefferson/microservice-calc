events {}

# Define which servers to include in the load balancing scheme.
#https://www.digitalocean.com/community/tutorials/understanding-nginx-server-and-location-block-selection-algorithms-pt

http {
    
   upstream ping {
        server microservice-calc_ping_1:5000;
        server microservice-calc_ping_2:5000;
     }

    upstream calc{
        server microservice-calc_calc_1:5000;
    }

    upstream soma{
        server microservice-calc_soma_1:5000;
    }

    upstream sub{
        server microservice-calc_sub_1:5000;
    }

# This server accepts all traffic to port 80 and passes it to the upstream.
     server {
         listen 80;
        
         server_name app.com;

         location /ping {
              proxy_pass http://ping;
         }

         location /calc {
              proxy_pass http://calc;
         }

         location /soma {
              proxy_pass http://soma;
         }

         location /sub {
              proxy_pass http://sub;
         }
     }
}
